
/**
 * main - checks for alphabetic character
 * Return: 0 if it is not a letter
 * Return: 1 if it is a letter
 *
**/

int _isalpha(int c)
{

	if(isalpha(c) == 0)
		return (0);
	else
		return (1);

}
