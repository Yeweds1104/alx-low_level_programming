#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
* main - prints lowercase alphabets
* Return: 0 if exited properly, non zero otherwise
 **/

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);

	putchar('\n');

	return (0);

}
